<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="DetailTableCell" parent="@android:style/TextAppearance.Small">
        <item name="android:padding">3dip</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="SettingsTitle" parent="@android:style/TextAppearance.Large">
        <item name="android:background">@drawable/item_settings_background_header_first</item>
        <item name="android:padding">10dp</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="SettingsRow">
        <item name="android:padding">10dp</item>
        <item name="android:background">@drawable/item_list_background</item>
    </style>

    <style name="SettingsRowTitle" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">@color/colorTextDark</item>
    </style>

    <style name="SettingsRowDescription">
        <item name="android:textColor">@color/colorTextGray</item>
        <item name="android:textSize">12sp</item>
    </style>

</resources>
